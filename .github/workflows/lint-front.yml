name: Ca lint à balle par chez vous

on:
 push:
  branches: [main]

jobs:
  Ca-lint-a-balle-par-chez-vous:
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v2
      - run: cd front
      - name: Installation des dépendances
        run: yarn
      - name: Lintage
        run: eslint . --ext .js

